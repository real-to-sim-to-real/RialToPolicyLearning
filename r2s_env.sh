# set variables as you wish + source this from the root directory!

# directory for saving wandb
# export WANDB_DIR=/data/scratch-oc40/pulkitag/marcel/
# export WANDB_DIR=/data/scratch-oc40/pulkitag/anthony/wandb
export WANDB_DIR=/home/anthony/wandb

# update python path (instead of `conda develop dependences`)
export PYTHONPATH=$PYTHONPATH:$PWD/dependencies
